import { SuppliersFilterPipe } from './suppliers-filter.pipe';

describe('SuppliersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SuppliersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
